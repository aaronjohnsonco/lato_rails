require 'spec_helper'

describe LatoRails do
  it 'has a version number' do
    expect(LatoRails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
